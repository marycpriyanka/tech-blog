

{{#each posts as |post|}}
<div class="card customBorder">
  <a href="/{{post.id}}">
    <div class="card-header header-bg">
      {{post.title}}
    </div>
  </a>
  <div class="card-body">
    <p class="card-text">Date created: {{format_date post.date_created}}</p>
  </div>
</div>
{{/each}}