{{#each posts as |post|}}
<div class="card">
  <div class="card-header">
      {{post.title}}
  </div>
  <div class="card-body"> 
    <p class="card-text">Date created: {{format_date post.date_created}}</p>
  </div>
</div>
{{/each}}